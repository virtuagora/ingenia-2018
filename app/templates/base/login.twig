{# set show_footer = true }}
{# set show_navbar = true #}
{# set body_class = '' #}

{% extends 'master.twig' %}

{% block title %}Login{% endblock %}
{% block body %}
  <div class="hero is-white is-fullheight is-login-window">
    <div class="hero-body has-text-centered">
      <div class="container">
        <div class="columns">
          <div class="column is-6-tablet is-offset-3-tablet is-4-desktop is-offset-4-desktop">
            <a href="{{base_url()}}">
              <img src="{{asset('img/ingenia-logo.svg')}}" class="image login-logo" alt="">
            </a>
            {% if error %}
              <div class="notification is-warning">
                {{ error }}
              </div>
            {% endif %}
            <br>
            <div id="loading" class="notification is-light has-text-centered">
              <span class="icon">
                <i class="fas fa-cog fa-spin"></i>
              </span>
              Cargando . . .
            </div>
<login message="{{subject.type}}" form-url="{{ path_for('runLogin') }}" fb-login-action="{{path_for('fbCallbackGet')}}" fb-app="191906834751311" sign-up-url="{{ path_for('showSignUp') }}" home-url="{{path_for('showHome')}}"></login>
            <br>
            <a href="{{base_url()}}" class="button is-white is-medium is-fullwidth is-300"><i class="fas fa-home fa-fw"></i>&nbsp;Volver al inicio</a>
            <br>
            <br>
            <a href="https://virtuagora.org"><img src="{{asset('img/logo-v.svg')}}" class="image is-32x32 is-centered" alt=""></a>
            <br>
            <p class="is-size-7">
              Hecho con
              <i class="fas fa-heart"></i>
              por
              <a href="https://virtuagora.org">Virtuágora</a><br>para Gabinete Joven</p>
          </div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}

{% block javascripts %}{% endblock %}